{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn app.backend.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
```

### 3. Update `requirements.txt` (make sure it's in root)
Your existing requirements.txt should work, just ensure it's in the root directory.

### 4. `runtime.txt` (in root directory) - Optional
```
python-3.11.0
```

## Step 2: Project Structure Should Look Like:
```
your-project/
├── app/
│   ├── backend/
│   │   ├── main.py
│   │   ├── chains.py
│   │   ├── portfolio.py
│   │   └── utils.py
│   └── frontend/
│       └── streamlit_app.py
├── resource/
│   └── my_portfolio.csv
├── .env.example
├── .gitignore
├── requirements.txt
├── Procfile
├── railway.json
└── runtime.txt
